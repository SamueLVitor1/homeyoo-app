import { StyleSheet } from 'react-native'

export const membroItemStyles = StyleSheet.create({
  membroItem: {
    alignItems: 'center'
  },
  avatar: {
    width: 78,
    height: 78,
    borderRadius: 24,
    marginBottom: 4
  },
  nome: {
    fontSize: 15,
    fontWeight: '500',
    color: '#222'
  }
})
