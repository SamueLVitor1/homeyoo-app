import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    backgroundColor: '#fff',
    padding: 16,
    borderRadius: 16,
    alignItems: 'center',
    width: 110,
    elevation: 2,
  },
  label: {
    fontWeight: '600',
    fontSize: 14,
    textAlign: 'center',
    marginVertical: 4,
  },
  value: {
    fontSize: 14,
    color: '#333',
  },
});
